/*const entradaNumero: string | null = prompt("digite um numero entre 1 a 12")

if (entradaNumero !== null) {
const numero = parseFloat(entradaNumero)

switch (true){
    case numero === 1:
        console.log("janeiro")
        break

    case numero === 2:
        console.log(`fevereiro`)
        break
        
    case numero === 3:
        console.log("março")
        break
        
    case numero === 4:
        console.log("abril")
        break
        
    case numero === 5:
        console.log("maio")
        break
        
    case numero === 6:
        console.log("junho")
        break
        
    case numero === 7:
        console.log("julho")
        break
        
    case numero === 8:
        console.log("agosto")
        break
        
    case numero === 9:
        console.log("setembro")
        break
        
    case numero === 10:
        console.log("outubro")
        break
        
    case numero === 11:
        console.log("novembro")
        break
        
    case numero === 12:
        console.log("dezembro")
        break

default:
}
}
*/

/*let contagemRegressiva = prompt("Digite um número para contagem regressiva");

while (contagemRegressiva >= 0) {
    console.log(contagemRegressiva)
    contagemRegressiva--
}

console.log('FIM')


*/


/*

function parOuImpar (entradaNumero: number) : void {

    if (entradaNumero % 2 == 0) {
        console.log(` o numero ${entradaNumero} e par`)
    }
    else{
        console.log(` o numero ${entradaNumero} e impar`)
}
}

const e_numero: string | null = prompt(`Digite o numero para saber se ele e par ou impar: `)
if (e_numero !== null) {


    const numero = parseFloat(e_numero)

    parOuImpar (numero)

}

*/
/*function verificarValor(): void{
    let executar = true;

    while (executar) {
        const entradaValor : string | null = prompt("digite um numero valido")

    if (entradaValor !== null) {

        let valor = parseInt ( entradaValor );

        if (valor < 0 || valor > 10 ) {
            valor = valor
            executar = true;
            alert (`numero ${valor} invalido `);

        }
    else {
        executar = false;
        let numero : number = valor;

        alert (`numero ${valor} valido`)

    }
}
    }
}
     verificarValor()
     console.log (`o numero digitado e valido.`)
    */

